version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - ./gradlew clean build awsEbsPackage
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - build/dist/quicknotes-ebs.zip